'''
    This function is able to take a pid/od txt file and then
        parse the data into lists that are then send to the webserver
'''
def text_parser(file_path):
    data = open(file_path, "r")
    
    return []